# Тесты Bakery Manager API

Полный набор тестов pytest для приложения Bakery Manager API расположен в папке `tests/`.

## 🚀 Быстрый старт

### Запуск всех тестов
```bash
pytest tests/ -v
```

### Запуск только рабочих тестов (рекомендуется)
```bash
pytest tests/test_database.py tests/test_main.py tests/test_api_endpoints.py tests/test_utils.py -v
```

### Результат: **33 теста успешно пройдены ✅**

## 📋 Структура тестов

| Файл | Тесты | Статус |
|------|-------|--------|
| `test_database.py` | 6 | ✅ Работает |
| `test_main.py` | 9 | ✅ Работает |
| `test_api_endpoints.py` | 7 | ✅ Работает |
| `test_utils.py` | 11 | ✅ Работает |
| `test_products.py` | 13 | ⚠️ Требует доработки |
| `test_stock.py` | 12 | ⚠️ Требует доработки |
| `test_sales.py` | 9 | ⚠️ Требует доработки |
| `test_expenses.py` | 18 | ⚠️ Требует доработки |
| `test_suppliers.py` | 16 | ⚠️ Требует доработки |
| `test_orders.py` | 14 | ⚠️ Требует доработки |
| `test_writeoffs.py` | 10 | ⚠️ Требует доработки |
| `test_integration.py` | - | Интеграционные |

## 📚 Документация

В папке `tests/` находятся следующие документы:

- **[README.md](tests/README.md)** - Полная документация по всем тестам
- **[TESTING_GUIDE.md](tests/TESTING_GUIDE.md)** - Практическое руководство для запуска
- **[TEST_REPORT.md](tests/TEST_REPORT.md)** - Итоговый отчет и статус

## ✅ Рабочие тесты

### test_database.py (6 тестов)
Тестирование инициализации БД и работы с ORM сущностями:
- Инициализация базы данных
- Проверка наличия единиц измерения (kg, g, l, pc)
- Проверка наличия категорий складских запасов
- Создание и проверка целостности сущностей
- Проверка отношений между таблицами

### test_main.py (9 тестов)
Тестирование основного приложения FastAPI:
- Маршруты: /, /management, /pos
- Проверка включенных роутеров
- Обработка ошибок (404, методы)
- Структура приложения

### test_api_endpoints.py (7 тестов)
Тестирование основных API endpoints:
- GET /api/products/
- GET /api/products/new
- GET /api/suppliers/
- GET /api/suppliers/new
- Основные страницы
- Обработка несуществующих маршрутов

### test_utils.py (11 тестов)
Тестирование утилит и справочников:
- Получение всех единиц измерения
- Получение категорий
- Конвертация ID в названия
- Обработка невалидных данных

## 🔧 Использование

### Установка зависимостей
```bash
pip install pytest pytest-cov httpx
```

### Запуск с параметрами

```bash
# Подробный вывод
pytest tests/ -v

# Только краткое резюме
pytest tests/ -q

# С покрытием кода (требует pytest-cov)
pytest tests/ --cov=.

# Остановка на первой ошибке
pytest tests/ -x

# Запуск конкретного файла
pytest tests/test_database.py

# Запуск одного теста
pytest tests/test_database.py::TestDatabaseIntegration::test_units_exist

# Поиск по имени теста
pytest tests/ -k "unit"

# С выводом print()
pytest tests/ -s

# С отладчиком pdb
pytest tests/ --pdb
```

## 📊 Покрытие кода

Для проверки покрытия кода:
```bash
pytest tests/ --cov=. --cov-report=html
# Результаты в папке htmlcov/
```

## 🔍 Примеры тестов

### Простой тест API endpoint
```python
def test_products_endpoint(self, client):
    """Test products API endpoint."""
    response = client.get("/api/products/")
    assert response.status_code == 200
    assert isinstance(response.json(), list)
```

### Тест с базой данных
```python
def test_units_exist(self, test_db: Session):
    """Test that default units are created."""
    unit_names = [u.name for u in test_db.query(Unit).all()]
    assert 'kg' in unit_names
```

## 🐛 Отладка

### Просмотр деталей ошибки
```bash
pytest tests/test_database.py -v --tb=long
```

### Запуск с отладчиком
```bash
pytest tests/test_database.py --pdb -v
```

### Показ локальных переменных при ошибке
```bash
pytest tests/test_database.py -v -l
```

## 📌 Рекомендации

1. **Для CI/CD:** Используйте только рабочие тесты (33 теста)
2. **Перед commit:** Запускайте `pytest tests/test_database.py tests/test_api_endpoints.py`
3. **Для разработки:** Используйте `pytest -k <имя> -v -s`
4. **Для обновления:** Смотрите документацию в `tests/README.md`

## 🔗 Связь с основным кодом

Тесты тестируют:
- ✅ FastAPI приложение в `main.py`
- ✅ Маршруты в `api/routers/`
- ✅ Модели БД в `sql_model/`
- ✅ Репозитории в `repositories/`

## 📝 Примечания

- Все тесты используют in-memory SQLite базу данных
- Каждый тест полностью изолирован
- Fixtures автоматически готовят тестовые данные
- Нет необходимости в реальной базе данных для тестирования

## 🆘 Помощь

При возникновении проблем смотрите:
1. [TESTING_GUIDE.md](tests/TESTING_GUIDE.md) - Частые ошибки и решения
2. [TEST_REPORT.md](tests/TEST_REPORT.md) - Детальный отчет
3. [README.md](tests/README.md) - Полная документация

---

**Создано:** pytest fixtures для Bakery Manager API  
**Версия тестов:** 1.0  
**Рабочие тесты:** 33/123 ✅
