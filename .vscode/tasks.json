{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build PyInstaller",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": [
                "-m", "PyInstaller",
                "--onefile",
                "${workspaceFolder}/main_app.py"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
