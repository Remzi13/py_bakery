# 🥐 Запуск Bakery Manager

## Для Windows (Git Bash / MSYS / PowerShell)

### Способ 1: Используя shell скрипт
```bash
bash run.sh
```

### Способ 2: Ручной запуск
```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

---

## Для Windows (Command Prompt)

### Способ 1: Используя батник
```cmd
run.bat
```

### Способ 2: Ручной запуск
```cmd
uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

---

## Для macOS / Linux

```bash
bash run.sh
```

---

## Что делают скрипты?

✅ Проверяют наличие Python  
✅ Проверяют наличие fastapi и uvicorn  
✅ Устанавливают зависимости если нужно  
✅ Открывают браузер на нужной странице  
✅ Запускают сервер с автоперезагрузкой  

---

## Опции запуска Uvicorn

| Опция | Описание |
|-------|---------|
| `--host 127.0.0.1` | Слушает только локальное подключение (по умолчанию в скриптах) |
| `--host 0.0.0.0` | Слушает все IP адреса (для сетевого доступа) |
| `--port 8000` | Порт сервера (по умолчанию) |
| `--reload` | Автоматический перезагрузка при изменении кода |
| `--workers 4` | Количество рабочих процессов |
| `--log-level debug` | Уровень логирования |

### Пример с другим портом:
```bash
uvicorn main:app --host 127.0.0.1 --port 5000 --reload
```

### Пример с сетевым доступом:
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### Пример с несколькими рабочими процессами:
```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --workers 4
```

---

## Проверка доступности

После запуска, откройте браузер и перейдите на:

- **http://127.0.0.1:8000** - основное приложение 🥐
- **http://127.0.0.1:8000/docs** - интерактивная документация (Swagger UI) 📚
- **http://127.0.0.1:8000/redoc** - ReDoc документация 📖
- **http://127.0.0.1:8000/openapi.json** - OpenAPI JSON схема

---

## Остановка сервера

Нажмите **Ctrl+C** в терминале / командной строке

---

## Решение проблем

### Ошибка: "Address already in use"
Порт 8000 уже занят. Используйте другой порт:
```bash
uvicorn main:app --host 127.0.0.1 --port 5000 --reload
```

Или найдите процесс на порту 8000:
```bash
# Linux/macOS
lsof -i :8000

# Windows PowerShell
netstat -ano | findstr :8000
```

### Ошибка: "ModuleNotFoundError: No module named 'fastapi'"
Установите зависимости:
```bash
pip install fastapi uvicorn
```

### Ошибка: "Permission denied" на Linux/macOS
Дайте права на выполнение скрипта:
```bash
chmod +x run.sh
bash run.sh
```

### Браузер не открывается автоматически
Откройте вручную: http://127.0.0.1:8000

### Сервер запускается но не видит изменения в коде
Убедитесь что используется флаг `--reload`:
```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

---

## Структура проекта

```
py_bakery/
├── run.sh                 ← Скрипт для запуска (Linux/macOS/Git Bash)
├── run.bat                ← Скрипт для запуска (Windows CMD)
├── RUN_SERVER.md          ← Инструкции по запуску
├── main.py                ← Главный файл FastAPI
├── api/
│   ├── dependencies.py
│   ├── models.py
│   └── routers/
│       ├── products.py
│       ├── stock.py
│       ├── sales.py
│       ├── expenses.py
│       ├── suppliers.py
│       ├── ingredients.py
│       └── writeoffs.py
├── repositories/          ← Слой доступа к данным
├── sql_model/             ← Модели БД
├── static/
│   ├── index.html         ← Главная страница
│   ├── dock.html          ← Документация
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── translations.js ← Поддержка языков
│       └── modules/
└── database.db            ← SQLite база данных
```

---

## Полезные команды

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Просмотр логов в реальном времени
```bash
uvicorn main:app --host 127.0.0.1 --port 8000 --reload --log-level debug
```

### Запуск без автоперезагрузки (продакшен)
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

---

**Версия**: 1.0  
**Последнее обновление**: January 8, 2026

---

## 🛠 Работа с миграциями базы данных (Alembic)

Проект настроен для использования **Alembic** для управления изменениями структуры базы данных.

### 1️⃣ Создание новой миграции
После того как вы изменили модели в `sql_model/entities.py`, выполните команду для автогенерации миграции:

```bash
# Windows
.\venv_build\Scripts\alembic revision --autogenerate -m "Описание изменений"

# Linux / macOS
./venv_build/bin/alembic revision --autogenerate -m "Описание изменений"
```

Это создаст новый файл миграции в папке `alembic/versions/`. Всегда проверяйте содержимое созданного файла!

### 2️⃣ Применение миграций
Чтобы обновить базу данных до актуального состояния:

```bash
# Windows
.\venv_build\Scripts\alembic upgrade head

# Linux / macOS
./venv_build/bin/alembic upgrade head
```

### 3️⃣ Откат миграций
Если нужно отменить последнюю миграцию:

```bash
# На один шаг назад
alembic downgrade -1
```

### 🗒 Примечание
Если вы используете `venv_build` (как настроено в проекте), используйте пути к python/alembic внутри него, либо активируйте окружение перед запуском команд.
